<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.registration.mapper.SysDictTypeMapper">

    <resultMap id="BaseResultMap" type="com.hospital.registration.entity.SysDictType">
        <id column="id" property="id"/>
        <result column="dict_type" property="dictType"/>
        <result column="dict_name" property="dictName"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据字典类型编码查询 -->
    <select id="selectByDictType" resultMap="BaseResultMap">
        SELECT * FROM sys_dict_type WHERE dict_type = #{dictType}
    </select>

    <!-- 查询所有启用的字典类型 -->
    <select id="selectAllEnabled" resultMap="BaseResultMap">
        SELECT * FROM sys_dict_type WHERE status = 1 ORDER BY id
    </select>

</mapper>